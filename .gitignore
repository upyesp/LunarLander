# Private / local-only — never commit
initial prompt.md

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Local dev tooling / diagnostics (never commit)
node_modules/
package.json
package-lock.json
_diag*.js
_shot_*.png
